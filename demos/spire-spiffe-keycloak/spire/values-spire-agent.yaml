# Path B (stretch) only. Draft values — [VERIFY] against
# `helm show values spiffe/spire-agent` for your chart version before using.
trustDomain: "openshell.demo"
workloadAttestors:
  k8s:
    enabled: true
