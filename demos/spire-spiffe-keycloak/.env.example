# Demo-specific variables for spire-spiffe-keycloak.
# Copy to .env alongside this file (gitignored). Real secrets never committed.

KEYCLOAK_HOST=                       # e.g. keycloak.apps.<cluster-domain>
KEYCLOAK_REALM=openshell
KEYCLOAK_CLIENT_ID_CLI=openshell-cli
KEYCLOAK_CLIENT_ID_GATEWAY=openshell-gateway
KEYCLOAK_CLIENT_SECRET=              # confidential client secret, never commit a real value
SPIRE_TRUST_DOMAIN=openshell.demo    # Path B only

# MCP servers (Path B extension) — see mcp-servers/README section in the
# demo README. Fill in your actual OCI image references.
MCP_SERVER_A_IMAGE=quay.io/atarazana/eligibility-engine-mcp-rs                  # e.g. ghcr.io/your-org/mcp-server-a
MCP_SERVER_A_TAG=2.0.2
MCP_SERVER_B_IMAGE=quay.io/atarazana/compatibility-engine-mcp-rs                  # e.g. ghcr.io/your-org/mcp-server-b
MCP_SERVER_B_TAG=3.1.1
KEYCLOAK_ADMIN_TOKEN=                # short-lived; obtain via your own admin login, never persist
