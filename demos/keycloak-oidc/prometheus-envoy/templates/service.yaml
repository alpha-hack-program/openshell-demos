apiVersion: v1
kind: Service
metadata:
  name: prometheus-envoy
  labels:
    app.kubernetes.io/name: prometheus-envoy
spec:
  selector:
    app.kubernetes.io/name: prometheus-envoy
  ports:
  - name: http
    port: 8080
    targetPort: 8080
