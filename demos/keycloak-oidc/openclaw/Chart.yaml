apiVersion: v2
name: openclaw
description: OpenClaw chat-driven frontend wired to this demo's OpenShell gateway (as alice) and Keycloak realm (via trusted-proxy SSO) — third-party/community, not production-ready upstream, see README caveats
type: application
version: 0.1.0
appVersion: "2026.7.1-2-slim"
