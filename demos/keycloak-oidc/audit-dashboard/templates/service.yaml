apiVersion: v1
kind: Service
metadata:
  name: audit-dashboard
  labels:
    app.kubernetes.io/name: audit-dashboard
spec:
  selector:
    app.kubernetes.io/name: audit-dashboard
  ports:
    - name: http
      port: 8080
      targetPort: http
